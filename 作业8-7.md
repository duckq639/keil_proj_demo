# 作业 2
1. uVision F7 会自动完成编译和链接（生成 `.axf`），这个格式基本上可以理解为 .elf，可以包含调试信息，符号表等内容
2. F8 负责烧录（一般会将 .axf 删掉调试信息等变为最简单的二进制 .bin 或者带有定位信息的 .hex）
3. 单片机加载程序是通过硬件 loader 加载入口语句（没有操作系统）


# 作业 5
1. 使用 uVision 打开 .uvprojx 文件
2. 在左侧 project 文件页上 add group 并放入 c 文件
3. 在 option for target 窗口中找到 C/C++ 并且调整 Include path（用来放入头文件）
4. 在 option for target 窗口中找到 Debug 并将 Use 设置为 ST-Link debugger
5. 使用 F7 编译，F8 烧录，Ctrl + F5 进入 Debug 模式，可以打断点